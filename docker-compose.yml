version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ahmet-portfolio
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
